import React from 'react';
interface FormLoaderProps {
    numRows?: number;
    width?: string;
}
export default function FormLoader({ numRows, width }: FormLoaderProps): React.JSX.Element;
export {};
